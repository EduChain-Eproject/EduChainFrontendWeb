import handleGetChapterDetail from "./handleGetChapterDetail"
import handleUpdateChapter from "./handleUpdateChapter"
import handleDeleteChapter from "./handleDeleteChapter"
import handleCreateChapter from "./handleCreateChapter"

export {
    handleGetChapterDetail,
    handleUpdateChapter,
    handleDeleteChapter,
    handleCreateChapter,
}